<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsh.erp.datasource.mappers.ProjectMapperEx">
    <select id="selectByConditionProject" parameterType="com.jsh.erp.datasource.entities.ProjectExample" resultMap="com.jsh.erp.datasource.mappers.ProjectMapper.BaseResultMap">
        select *
        FROM jsh_project
        where 1=1
        <if test="name != null and name != ''">
            <bind name="bindName" value="'%'+name+'%'"/>
            and name like #{bindName}
        </if>
        <if test="type != null and type != ''">
            and type=#{type}
        </if>
        <if test="customer != null and customer != ''">
            and customer=#{customer}
        </if>
        and ifnull(delete_flag,'0') !='1'
        order by id desc
        <if test="offset != null and rows != null">
            limit #{offset},#{rows}
        </if>
    </select>
    <select id="countsByProject" resultType="java.lang.Long">
        SELECT
        COUNT(id)
        FROM jsh_project
        WHERE 1=1
        <if test="name != null and name != ''">
            <bind name="bindName" value="'%'+name+'%'"/>
            and name like #{bindName}
        </if>
        <if test="type != null and type != ''">
            and type=#{type}
        </if>
        <if test="customer != null and customer != ''">
            and customer=#{customer}
        </if>
        and ifnull(delete_flag,'0') !='1'
    </select>
    <update id="batchDeleteProjectByIds">
        update jsh_project
        set delete_flag='1'
        where 1=1
        and id in (
        <foreach collection="ids" item="id" separator=",">
            #{id}
        </foreach>
        )
    </update>
</mapper>