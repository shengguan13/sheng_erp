<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jsh.erp.datasource.mappers.DepotAllocationMapperEx">

    <resultMap extends="com.jsh.erp.datasource.mappers.DepotAllocationMapper.BaseResultMap" id="ResultAndInfoMap" type="com.jsh.erp.datasource.entities.DepotAllocationVo4Depot">
        <result column="depot_name" jdbcType="VARCHAR" property="depotName" />
    </resultMap>

    <select id="selectDepotAllocation"  resultMap="ResultAndInfoMap">
        select da.*, de.name depot_name from jsh_depot_allocation da
        left join jsh_depot de on da.depot_id=de.id and ifnull(de.delete_Flag,'0') !='1'
        where 1=1
        <if test="allocation != null">
            <bind name="bindAllocation" value="'%'+allocation+'%'"/>
            and da.allocation like #{bindAllocation}
        </if>
        <if test="type != null">
            <bind name="bindType" value="'%'+type+'%'"/>
            and da.type like #{bindType}
        </if>
        <if test="depotId != null">
            and da.depot_id = #{depotId}
        </if>
        and ifnull(da.delete_flag,'0') !='1'
        order by id desc
        <if test="offset != null and rows != null">
            limit #{offset},#{rows}
        </if>
    </select>

    <select id="countDepotAllocation" resultType="java.lang.Long">
        select count(da.id) from jsh_depot_allocation da
        left join jsh_depot de on da.depot_id=de.id and ifnull(de.delete_Flag,'0') !='1'
        where 1=1
        <if test="allocation != null">
            <bind name="bindAllocation" value="'%'+allocation+'%'"/>
            and da.allocation like #{bindAllocation}
        </if>
        <if test="type != null">
            <bind name="bindType" value="'%'+type+'%'"/>
            and da.type like #{bindType}
        </if>
        <if test="depotId != null">
            and da.depot_id = #{depotId}
        </if>
        and ifnull(da.delete_flag,'0') !='1'
    </select>

    <update id="batchDeleteDepotAllocationByIds">
        update jsh_depot_allocation
        set delete_flag='1'
        where 1=1
        and id in (
        <foreach collection="ids" item="id" separator=",">
            #{id}
        </foreach>
        )
    </update>
</mapper>